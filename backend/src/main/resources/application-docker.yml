spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres:5432/recall}
  ai:
    ollama:
      base-url: ${OLLAMA_BASE_URL:http://ollama:11434}

recall:
  cors-origin: ${CORS_ORIGIN:http://localhost:3000}